---
version: '3'
services:
  elastic_search:
    image: ssense/elasticsearch:latest
    volumes:
      - $PWD/target/data:/data
    privileged: true
    hostname: elastic
    ports:
      - "9250:9200"
      - "9350:9300" 
    healthcheck:
      test: "curl -f http://localhost:9200"

  
